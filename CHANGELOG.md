# CHANGELOG

## [v0.4.0] - 2026-08-16

### 🚀 性能优化 + 三层架构重构

- **性能**：攻略检索只做一次 + 内存缓存复用（原每步各查一次）——生成耗时 124s → 78s（提速 37%），功能无损
- **重构**：项目按功能分层为 `core/`（核心 Agent 架构）、`services/`（外部服务集成）、`app/`（产品层：接口+前端）三层目录
- **工程**：`build_kb.py` 块 id 改为文件名前缀（攻略统计/删除按前缀精确操作）

## [v0.3.0] - 2026-08-16

### 🖥️ Vue 3 前端 + 流式输出 + 攻略管理

- **前端重写**：Vue 3 + Vite 组件化（表单 / Trace / 结果 / 历史 / 攻略管理），API 开发代理
- **流式输出**：后端 `run_task_stream` 生成器 + SSE（NDJSON 逐事件），前端 ReadableStream 实时渲染——生成过程可见（约束→计划→每步执行→评审→打回）
- **攻略上传**：网页上传 txt/pdf → 提取文本 → 入库 → 立即可检索（复用百炼 embedding）
- **攻略管理**：已有攻略列表（文件名 + 块数）+ 删除（清 chroma 块 + 删源文件）
- **一键启动**：`start.ps1`（后端 + 前端 + 自动打开浏览器）

## [v0.2.0] - 2026-08-16

### 🌐 网页版 + RAG 攻略知识库 + 独立仓库

- **网页版**：FastAPI 后端（生成 / 保存 / 历史接口）+ 前端页面（表单 / Trace / Markdown 结果 / 历史管理）
- **历史保存**：行程存 SQLite（trips.db），支持查看/删除
- **RAG 攻略检索**：预置城市攻略（杭州/石家庄），执行者生成行程前先检索攻略（三重混合：向量 + BM25 + RRF + rerank）——景点/价格有真实依据，不靠模型编
- **独立仓库**：travel-agent 独立 GitHub 仓库上架

## [v0.1.0] - 2026-08-15

### 🧠 初始版本：手搓多 Agent（规划-执行-评审）

- **多 Agent 架构**：规划者（拆步骤）→ 执行者（真实工具：Open-Meteo 天气 / Nominatim 地理编码 / 计算）→ 评审者（把关），流程代码控制
- **评审打回**：评审不通过自动打回重做（最多 3 轮）——实测"下雨"case 第 1 轮打回、第 2 轮修正通过
- **代码兜底**：
  - 约束正则解析（城市/天数/预算数字不经过模型，0 幻觉——实测预算 1000 不会被改成 800）
  - 安全拦截（"付款/银行卡/身份证"等高风险请求代码级终审，human-in-the-loop）
- **验收体系**：5 类 case（常规规划 / 预算约束 / 偏好约束 / 天气变化 / 安全边界）全部通过
- **前置阶段**：先用 LangChain `create_agent` 快速跑通验证（v0.1 前身），再手搓三角色复现其内部（节点/边/状态 = LangGraph 思想）
